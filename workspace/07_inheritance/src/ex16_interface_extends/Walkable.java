package ex16_interface_extends;

// 인터페이스의 이름은 ~할 수 있는(~able) 의 형태를 가지는 경우가 있다.

// Pet 중에서 산책할 수 있는 Pet만 골라서,
// Walkable 인터페이스를 구현해 준다.

public interface Walkable {

	// 인터페이스를 구현할 수 있는 클래스 : Dog 클래스'만'  [ implement Walkable (* 인터페이스를 '타입'으로 사용 *) ]
	// Walkable pet (객체 생성) 시에_
	// Snake클래스는 포함이 되지 않는다!.[ implement Walkable (* 적용하지 않아서! *) ]
	
	
	// implement Walkable
	// 	: Dot 클래스
	
	
	
	
	
}
